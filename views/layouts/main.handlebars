<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Travel</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Oswald&display=swap" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <section class="hero is-dark">
    <div class="hero-head">
      <nav class="navbar has-background-dark" role="navigation" aria-label="main navigation">
        <div class="container">
          <div class="navbar-brand">
            <h1 class="web-header title">TRAVEL BUDDY</h1>
            <span class="navbar-burger" aria-label="menu" data-target="navbarMenuHeroA">
              <span></span>
              <span></span>
              <span></span>
            </span>
          </div>
          <div id="navbarMenuHeroA" class="navbar-menu">
            <div class="navbar-end">
              {{#if loggedIn}}
              <a class="navbar-item has-text-white-bis has-background-dark" href="/">Home</a>
              <a class="navbar-item has-text-white-bis has-background-dark" href="/controllers/home-routes.js">
                Add new attraction
              </a>
              <button class="navbar-item button has-background-grey-light" id="logout">Logout</button>
              {{else}}
              <a class="navbar-item" href="/login">
                Sign up
              </a>
              <a class="navbar-item">
                Log In
              </a>
              {{/if}}
            </div>
          </div>
        </div>
      </nav>
    </div>
  </section>
  <main>
    <section>
      <div class="columns">
        <div class="column is-one-fifth">
          <aside class="description"> Simply search your location and find activities suitable to your needs!</aside>
        </div>
        <div class="continents column is-four-fifths is-flex-direction-row">
          {{{body}}}
        </div>
      </div>
    </section>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>