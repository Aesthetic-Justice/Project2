<section>
<h3 class="continents-name">{{continents.name}}</h3>
<div class="column is-half">
 <section class="flex-row justify-center">
    {{#each continents.countries as |country|}}
    {{> country}}
    {{/each}}
  </section>
  </div>
</div>
</section>