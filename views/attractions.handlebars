<section>
<div class="card is-centered">
<header class="card-name">{{cities.name}}</header>
<div class="column">
 <section class="flex-row justify-center card-content">
    {{#each cities.attractions as |attraction|}}
    {{> attraction-details}}
    {{/each}}
  </section>
  </div>
</div>
</section>