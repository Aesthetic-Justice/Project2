<section>
<h3 class="city-name">{{cities.name}}</h3>
<div class="column">
 <section class="flex-row justify-center">
    {{#each cities.attractions as |attraction|}}
    {{> attraction-details}}
    {{/each}}
  </section>
  </div>
</div>
</section>